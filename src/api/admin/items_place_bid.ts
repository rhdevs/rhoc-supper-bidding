// import { DocumentData, QueryDocumentSnapshot } from "@firebase/firestore";
// import { db } from "@/firebase.ts";

// const placeBid = async (groupRef: QueryDocumentSnapshot<DocumentData>, itemRef: QueryDocumentSnapshot<DocumentData>, pointsBid: number) => {
//   if (groupRef.data().points < pointsBid) {
//     throw new Error("Not enough points");
//   }
  
// }